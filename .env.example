# =============================================================================
# Tecnotouch Vending Machine Dashboard - Unified Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# Works for both local execution and Docker deployment
# =============================================================================

# -----------------------------------------------------------------------------
# VENDING MACHINE CONNECTION
# -----------------------------------------------------------------------------
# Choose one of these values based on your setup:
#
# TESTING/DEVELOPMENT (with simulator):
#   DISTRIBUTOR_IP=localhost
#   - Local execution: connects to localhost:1500 simulator
#   - Docker execution: auto-converts to "simulator" container
#
# PRODUCTION (real vending machine):
#   DISTRIBUTOR_IP=192.168.1.65
#   - Works the same in local and Docker execution
#
# -----------------------------------------------------------------------------

# Vending Machine IP Address (see options above)
DISTRIBUTOR_IP=localhost

# Vending Machine Port (default: 1500, usually no need to change)
DISTRIBUTOR_PORT=1500

# Admin Password (used for authentication)
DISTRIBUTOR_PASSWORD=your_admin_password

# -----------------------------------------------------------------------------
# SERVER PORTS
# -----------------------------------------------------------------------------

# Backend API Port
API_PORT=8000

# Frontend Port (where you access dashboard in browser)
FRONTEND_PORT=3000

# API Host (use 0.0.0.0 to allow remote access)
API_HOST=0.0.0.0

# -----------------------------------------------------------------------------
# DATABASE
# -----------------------------------------------------------------------------

# Database path
# - Local execution: relative path (sales_data.db)
# - Docker: absolute path (/app/data/sales_data.db) managed by volume
DB_PATH=sales_data.db

# -----------------------------------------------------------------------------
# FRONTEND BUILD VARIABLES (for Docker build)
# -----------------------------------------------------------------------------

# App Title (shown in browser tab)
VITE_APP_TITLE=Dashboard Distributore

# App Version
VITE_APP_VERSION=2.0.0

# =============================================================================
# QUICK START EXAMPLES
# =============================================================================
#
# LOCAL EXECUTION (without Docker):
# -----------------------------------
# 1. Testing with simulator:
#    Set: DISTRIBUTOR_IP=localhost
#    Terminal 1: python3 simulator/vending_machine_simulator.py
#    Terminal 2: python3 backend/api_server.py
#    Access: http://localhost:3000
#
# 2. Production with real machine:
#    Set: DISTRIBUTOR_IP=192.168.1.65
#    Run: python3 backend/api_server.py
#    Access: http://localhost:3000
#
# DOCKER EXECUTION:
# -----------------
# 1. Testing with simulator:
#    Set: DISTRIBUTOR_IP=localhost (auto-converts to simulator container)
#    Run: docker-compose --profile simulation up
#    Access: http://localhost:3000
#
# 2. Production with real machine:
#    Set: DISTRIBUTOR_IP=192.168.1.65
#    Run: docker-compose up
#    Access: http://localhost:3000
#
# =============================================================================
